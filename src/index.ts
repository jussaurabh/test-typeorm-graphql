// BASIC - basic-register-setup
/**
 * this branch contains the basic setup for registering user
 * with Setup of creating multiple resolvers and schemas
 * and merging them using graphql-tools(makeExecutableSchemas, mergeSchemas)
 */

import 'reflect-metadata';
import { startServer } from './startServer';

startServer();
